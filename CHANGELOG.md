# صفحه تغییرات

هر اپشن جدیدی که به FontWoW اضافه می‌شه، اینجا ثبت می‌شه.

> نسخه و یادداشت انتشار رسمی برنامه از `src/shared/updates.js` خوانده می‌شود. این فایل برای ثبت
> جزئیات توسعه‌ای تکمیلی است و نباید با آن منبع مغایرت داشته باشد.

## در حال توسعه

### افزودن ۶ فونت واقعی به فهرست اهداف
- فهرست «اهداف بعدی» با ۶ فونت واقعی از fontiran.com پر شد: سپیدار، طرلان، ایران‌سنس، دامون، سمرقند و کولاک.
- قیمت هر فونت برابر با قیمت لایسنس نسخه‌ی نامحدود (Unlimited License) همون فونت روی fontiran.com هست و کنار مبلغ هم نوشته شده «(نامحدود)» تا مشخص باشه.
- تصویر پیش‌نمایش هر فونت از صفحه‌ی رسمی‌ش دانلود و در `public/goals/` ذخیره شده (به‌جای hotlink به fontiran.com).
- اسم هر فونت به صفحه‌ی خریدش روی fontiran.com لینک شده.
- فایل‌های تغییر یافته: [`src/goals.js`](src/goals.js), [`src/Landing.jsx`](src/Landing.jsx), [`src/Landing.css`](src/Landing.css), [`public/goals/`](public/goals)

### اهداف بعدی و آخرین حمایت‌های مالی
- بخش «اهداف بعدی» به صفحه‌ی لندینگ اضافه شد: فهرست فونت‌های در نوبت خرید به‌ترتیب اولویت، همراه با قیمت، مبلغ جمع‌شده و نوار پیشرفت درصدی.
- بخش «آخرین حمایت‌های مالی» اضافه شد: مبالغ تومانی آخرین دونیت‌های دریافتی (بدون نام یا پیام دونیت‌کننده) را نشان می‌دهد، با تأکید بر اینکه برای پروژه‌ی FontWoW ثبت شده‌اند.
- داده‌ی دونیت‌ها به‌جای فراخوانی مستقیم API دارمت از سمت مرورگر (که نیاز به افشای توکن دارد)، هر ۳ ساعت با یک GitHub Action زمان‌بندی‌شده و توکن مخفی (`DARAMET_TOKEN` در Secrets) گرفته می‌شود و در `public/donations.json` نوشته می‌شود؛ فرانت‌اند فقط همین فایل استاتیک را می‌خواند.
- فایل‌های تغییر یافته: [`src/goals.js`](src/goals.js), [`src/Landing.jsx`](src/Landing.jsx), [`src/Landing.css`](src/Landing.css), [`scripts/fetch-donations.mjs`](scripts/fetch-donations.mjs), [`.github/workflows/donations.yml`](.github/workflows/donations.yml), [`public/donations.json`](public/donations.json)

### افزودن لایه‌ی عکس/استیکر
- کنار دکمه‌ی «＋ Aa» (افزودن لایه‌ی متن)، یک دکمه‌ی جدید برای آپلود عکس/استیکر از گالری گوشی اضافه شد.
- لایه‌ی عکس مثل لایه‌ی متن قابل جابجایی (drag) و چرخش (rotate) است؛ علاوه بر این با دکمه‌ی گرد پایین-راست قابل بزرگ‌تر شدن هم هست.
- فایل‌های تغییر یافته: [`src/App.jsx`](src/App.jsx), [`src/App.css`](src/App.css), [`src/strings.js`](src/strings.js), [`README.md`](README.md)
