# مشارکت در FontWoW

از مشارکت شما استقبال می‌کنیم. FontWoW یک پروژهٔ متن‌باز، سمت کاربر و موبایل‌محور است؛
تغییرات باید حریم خصوصی، تجربهٔ فارسی/انگلیسی و کیفیت خروجی تصویر را حفظ کنند.

## پیش از شروع

- برای تغییرات بزرگ ابتدا یک Issue باز کنید تا دامنه و طراحی راه‌حل هماهنگ شود.
- برای باگ‌های امنیتی Issue عمومی نسازید و از [سیاست امنیت](SECURITY.md) پیروی کنید.
- [راهنمای توسعه](docs/DEVELOPMENT.md) و [معماری](docs/ARCHITECTURE.md) را بخوانید.
- با ارسال مشارکت، رعایت [منشور رفتاری](CODE_OF_CONDUCT.md) را می‌پذیرید.

## راه‌اندازی

```bash
git clone https://github.com/FontWoW/FontWoW.github.io.git
cd FontWoW.github.io
npm install
npm run dev
```

## گردش کار پیشنهادی

1. یک شاخهٔ کوتاه و مشخص مانند `fix/export-spacing` یا `feat/font-search` بسازید.
2. تغییر را محدود به مسئلهٔ موردنظر نگه دارید.
3. متن‌های رابط را در هر دو زبان `fa` و `en` داخل `src/shared/strings.js` اضافه کنید.
4. برای قابلیت یا رفع باگ کاربرمحور، نسخه و یادداشت انتشار را طبق
   [راهنمای انتشار](docs/RELEASING.md) به‌روز کنید.
5. بررسی‌های [راهنمای تست](docs/TESTING.md) را انجام دهید.
6. Pull Request را با توضیح مسئله، راه‌حل، روش تست و تصویر تغییرات بصری ارسال کنید.

## استاندارد تغییرات

- سبک موجود JavaScript/JSX و CSS را دنبال کنید؛ پروژه TypeScript نیست.
- وابستگی جدید فقط زمانی اضافه شود که ارزش آن از هزینهٔ حجم، نگه‌داری و ریسک امنیتی بیشتر باشد.
- کنترل‌های ویرایشگر نباید وارد `.stage-inner` شوند؛ این عنصر مستقیماً خروجی PNG می‌شود.
- تجربهٔ موبایل، لمس، RTL/LTR، دسترس‌پذیری و `prefers-reduced-motion` را بررسی کنید.
- فایل‌های تولیدشده، اسرار، توکن‌ها و تنظیمات محلی را commit نکنید.

## بررسی نهایی

```bash
npm run lint
npm run build
```

پروژه فعلاً تست خودکار ندارد؛ Pull Request باید تست دستی انجام‌شده را شفاف بنویسد. جزئیات
در [docs/TESTING.md](docs/TESTING.md) آمده است.

## زبان و ارتباط

Issue و Pull Request می‌تواند فارسی یا انگلیسی باشد. متن رابط کاربری و یادداشت انتشار باید
در هر دو زبان کامل و هم‌معنا باشند.
